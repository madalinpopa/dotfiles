## Dotfiles

This is a repository with all my dotfiles.

### Steps to install
1. Create a python virtual environment `python3 -m venv .venv`
2. Enable python environment `source .venv/bin/activate`
3. Install requirements.txt `pip install -r requirements`
4. Run `main.py`
