{
    // =============================================
    // Editor settings
    // =============================================
    "editor.fontSize": 16.0,
    "editor.renderWhitespace": "All",
    // =============================================
    // Files settings
    // =============================================
    "files.exclude": [
        ".DS_Store",
        ".git",
        ".venv",
        "node_modules",
    ],
    // =============================================
    // Terminal settings
    // =============================================
    "terminal.fontSize": 16.0,
    "terminal.bell": false,
    // =============================================
    // UI settings
    // =============================================
    "theme": "light"
}
